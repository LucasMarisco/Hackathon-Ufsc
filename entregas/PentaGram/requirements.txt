# Dependências do pipeline de análise.
#
# Versões alinhadas com a imagem oficial do `analyzer-env/` da organização
# (python:3.11-slim). Mudar um pin aqui muda o que as ferramentas detectam —
# e achado diferente significa score diferente. Trate como parte do contrato
# de determinismo do pipeline, não como detalhe de ambiente.

# setuptools: semgrep ainda importa pkg_resources em runtime
setuptools

# --- ferramentas de análise estática (Python) ---
bandit==1.7.9
radon==6.0.1
pylint==3.3.1
semgrep==1.93.0

# --- pipeline ---
pydantic==2.8.2

# --- relatório interpretativo opcional via Gemini ---
google-genai
python-dotenv

# --- testes ---
# Não vem na imagem da organização, mas `tests/test_scoring.py` é entrega
# obrigatória (cenario-do-desafio.md) e vale nota em "qualidade de código".
pytest==8.2.2
